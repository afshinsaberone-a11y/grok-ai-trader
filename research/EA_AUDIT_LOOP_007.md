# لوپ رفع ایراد — GRK-FX-2026-007

هدف: تا وقتی `python research/project_repair_loop.py` روی قهرمان `GRK_Hybrid_Regime_EA.mq5` خروجی PASS ندهد، کد را اصلاح کن.

## ایرادهای باقی‌مانده از v2.20 که در v2.30 رفع شد

1. جهت ترند فقط با EMA بود؛ +DI/−DI اضافه شد تا ویپ‌ساو کم شود.
2. پس از ضرر پشت‌سرهم ورود فوری انجام می‌شد — کول‌داون میله اضافه شد.
3. مدارشکن فقط ضرر روزانه بود — اوج اکوئیتی هم قفل می‌کند.
4. هدف رنج می‌توانست از اسپرد کوچک‌تر باشد — حداقل چند برابر اسپرد الزامی شد.
5. چک‌لیست استاتیک موارد جدید را پوشش نمی‌داد — لوپ سخت‌گیرانه‌تر شد.

## لوپ

```bash
python research/project_repair_loop.py
```

پس از PASS استاتیک، در MetaEditor کامپایل کن و بک‌تست تیک روی EURUSD H1 بگیر.
لوپ استاتیک سودآوری زنده را اثبات نمی‌کند.
