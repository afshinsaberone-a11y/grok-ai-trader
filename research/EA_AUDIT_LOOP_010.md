# لوپ رفع ایراد — GRK-FX-2026-010

هدف: تا وقتی `python research/project_repair_loop.py` روی قهرمان
`ea/GRK_Hybrid_Regime_EA.mq5` خروجی PASS ندهد، کد را اصلاح کن.

لوپ استاتیک سودآوری زنده را اثبات نمی‌کند. فقط قرارداد ایمنی اجرا را چک می‌کند.

## ایرادهای ثبت‌شده تا ۲.۵۰

1. کول‌داون روی بستن برنده RANGE — رفع ۲.۴۰
2. فیلتر اسپرد مطلق بدون نسبت به ATR — رفع ۲.۴۰
3. اردر بدون چک مارجین آزاد — رفع ۲.۴۰
4. نبود رژیم COMPRESS — رفع ۲.۴۰
5. نبود هوک Deal برای ضرر — رفع ۲.۴۰
6. نبود MagicNumber / فیلتر پوزیشن — رفع ۲.۴۰
7. نبود سقف همزمانی پوزیشن — رفع ۲.۴۰
8. نبود پنجره سیاه خبر — هدف ۲.۵۰
9. نبود قفل ضرر متوالی — هدف ۲.۵۰

## دستور

```bash
python research/project_repair_loop.py
```

پس از PASS: کامپایل در MetaEditor و بک‌تست تیک EURUSD H1 با اسپرد واقعی.
