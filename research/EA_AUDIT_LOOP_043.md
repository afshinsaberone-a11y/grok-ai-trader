# لوپ ممیزی قرارداد ایمنی — GRK-FX-2026-043

هدف لوپ: EA بدون الگوی خطرناک (گرید/مارتینگل/میانگین ضرر)، با حد ضرر، سایز ریسک، فیلتر مجیک، گیت اسپرد و سقف ضرر روزانه.

```bash
python research/project_repair_loop.py --root .
python research/project_repair_loop.py --root . --fix --max-loops 8
```

لوپ **سود زنده را تضمین نمی‌کند**. فقط قرارداد ایمنی را چک و در صورت `--fix` از منبع کانونیکال پچ می‌کند.
