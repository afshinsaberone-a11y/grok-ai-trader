# لوپ رفع ایراد — GRK-FX-2026-008

هدف: تا وقتی `python research/project_repair_loop.py` روی قهرمان
`GRK_Hybrid_Regime_EA.mq5` خروجی PASS ندهد، کد را اصلاح کن.

## ایرادهای ۲.۳۰ که در ۲.۴۰ رفع شد

1. کول‌داون روی هر بستن RANGE اعمال می‌شد حتی اگر برنده بود.
2. فیلتر اسپرد فقط پوینت مطلق بود؛ نسبت به ATR اضافه شد.
3. ارسال اردر بدون چک مارجین آزاد.
4. رژیم COMPRESS (فشردگی→انبساط کنترل‌شده) وجود نداشت.
5. نتیجه معامله از تیکنت رصد نمی‌شد.

## لوپ

```bash
python research/project_repair_loop.py
```

پس از PASS استاتیک، در MetaEditor کامپایل کن و بک‌تست تیک روی EURUSD H1 بگیر.
لوپ استاتیک سودآوری زنده را اثبات نمی‌کند.
