# لوپ رفع ایراد — GRK-FX-2026-009

هدف: تا وقتی `python research/project_repair_loop.py` روی قهرمان
`GRK_Hybrid_Regime_EA.mq5` خروجی PASS ندهد، کد را اصلاح کن.

## ایرادهای ثبت‌شده تا ۲.۵۰

1. کول‌داون روی بستن برنده RANGE (رفع در ۲.۴۰).
2. فیلتر اسپرد مطلق بدون نسبت به ATR (رفع در ۲.۴۰).
3. ارسال اردر بدون چک مارجین آزاد (رفع در ۲.۴۰).
4. نبود رژیم COMPRESS (رفع در ۲.۴۰).
5. نبود هوک معامله در سطح Deal (رفع در ۲.۴۰).
6. نبود MagicNumber ثابت برای فیلتر پوزیشن‌های خود ربات (هدف ۲.۵۰).
7. نبود محدودیت همزمانی پوزیشن روی همین نماد+مجیک (هدف ۲.۵۰).

## لوپ

```bash
python research/project_repair_loop.py
```

پس از PASS استاتیک، در MetaEditor کامپایل کن و بک‌تست تیک روی EURUSD H1 بگیر.
لوپ استاتیک سودآوری زنده را اثبات نمی‌کند.
