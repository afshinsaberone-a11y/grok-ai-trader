# Cost-aware Gate v7

Strict fail-closed gate is active in `research/optimization/cost_aware_gate_v7.py`.

Pre-OOS 2022-2024: PF >= 1.05 every year, >=100 trades every year, DD <=35%, and positive expectancy in at least 2 years.
Validation 2025: PF >=1.10, DD <=35%, >=100 trades.
2026 is held out and must never be loaded by discovery.
