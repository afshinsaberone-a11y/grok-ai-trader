# حلقه ارتقای ربات طلا/دلار

اتومیشن روزانه باید:
1. فایل‌های فعلی `ea/GRK_XAUUSD_Gold_Dollar_EA.mq5` و `strategies/xauusd_gold_dollar_v1.py` را از ریپو بخواند.
2. یک ارتقای کوچک و قابل‌تست اعمال کند (نه بازنویسی کامل).
3. نسخه را افزایش دهد (1.0 → 1.1 → 1.2 ...).
4. changelog را در همین پوشه به‌روز کند.
5. به `main` پوش کند.
6. هیچ داده بازار جعلی نسازد و سود را تضمین نکند.

## Changelog

### v3.7 — 2026-09-24
- ارتقا: فیلتر دوشنبه ساعت اول لندن فقط با نسبت اسپرد به ATR نسبت به میانه ۲۰روزه همان ساعت (07–08 UTC).
- پایتون: `monday_london_spread_atr_med_mult=1.50`، `monday_london_spread_atr_lookback=20`. ستون `monday_london_spread_atr_med_block` = دوشنبه و ساعت لندن و `spread_used/ATR >= 1.50 * میانه ۲۰روزه همان نسبت در همان ساعت`. اسپرد جعلی ساخته نمی‌شود.
- ورود دوشنبه 07–08 UTC بسته است اگر نسبت اسپرد/ATR نسبت به تاریخچه همان ساعت بالا باشد.
- EA: `MondayLondonSpreadAtrMedMult=1.50`، `MondayLondonSpreadAtrLookback=20` و `MondayLondonSpreadAtrMedBlock()` نسبت اسپرد زنده ASK-BID به ATR را با میانه iSpread ساعتی واقعی دوشنبه‌ها مقایسه می‌کند. نسخه 3.70.
- ریسک پایه ۰.۵٪. سود تضمین نیست. معامله زنده فعال نشد.
- مورد بعدی: فیلتر دوشنبه ساعت اول لندن نسبت اسپرد/ATR بالا AND حجم نازک همان ساعت.

### v3.6 — 2026-09-24
- ارتقا: فیلتر دوشنبه ساعت اول لندن فقط وقتی اسپرد گشاد AND حجم نازک باشد (07–08 UTC).
- پایتون: `monday_london_thin_vol_ratio=0.70`، `monday_london_vol_lookback=20`. ستون `monday_london_thin_wide_block` = دوشنبه و ساعت لندن و `spread_used >= monday_london_wide_spread_atr * ATR` و حجم همان ساعت < نسبت میانه ۲۰روزه. اگر ستون حجم نباشد فیلتر AND خاموش است. اسپرد/حجم جعلی ساخته نمی‌شود.
- ورود دوشنبه 07–08 UTC دیگر فقط با اسپرد جداگانه بسته نمی‌شود؛ شرط AND هم اعمال می‌شود.
- EA: `MondayLondonThinVolRatio=0.70`، `MondayLondonVolLookback=20` و `MondayLondonThinWideBlock()` اسپرد زنده ASK-BID را با ATR و iVolume ساعتی واقعی دوشنبه‌ها مقایسه می‌کند. نسخه 3.60.
- ریسک پایه ۰.۵٪. سود تضمین نیست. معامله زنده فعال نشد.
- مورد بعدی: فیلتر دوشنبه ساعت اول لندن فقط با نسبت اسپرد به ATR نسبت به میانه ۲۰روزه همان ساعت.

### v3.5 — 2026-09-24
- ارتقا: فیلتر دوشنبه پس از گپ آخر هفته فقط با اسپرد واقعی نسبت به ATR در ساعت اول لندن (07–08 UTC).
- پایتون: `monday_london_wide_spread_atr=0.10`. ستون `monday_london_wide_block` = weekday دوشنبه و ساعت لندن و `spread_used >= monday_london_wide_spread_atr * ATR`. اسپرد جعلی ساخته نمی‌شود.
- ورود دوشنبه 07–08 UTC فقط اگر اسپرد واقعی نسبت به ATR گشاد باشد بسته می‌شود.
- EA: `MondayLondonWideSpreadAtr=0.10` و `MondayLondonWideBlock()` اسپرد زنده ASK-BID را با ATR واقعی مقایسه می‌کند. نسخه 3.50.
- ریسک پایه ۰.۵٪. سود تضمین نیست. معامله زنده فعال نشد.
- مورد بعدی: فیلتر دوشنبه ساعت اول لندن اسپرد گشاد AND حجم نازک.
