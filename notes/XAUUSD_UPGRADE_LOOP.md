# حلقه ارتقای ربات طلا/دلار

اتومیشن روزانه باید:
1. فایل‌های فعلی `ea/GRK_XAUUSD_Gold_Dollar_EA.mq5` و `strategies/xauusd_gold_dollar_v1.py` را از ریپو بخواند.
2. یک ارتقای کوچک و قابل‌تست اعمال کند (نه بازنویسی کامل).
3. نسخه را افزایش دهد (1.0 → 1.1 → 1.2 ...).
4. changelog را در همین پوشه به‌روز کند.
5. به `main` پوش کند.
6. هیچ داده بازار جعلی نسازد و سود را تضمین نکند.

اولویت ارتقا:
- فیلتر هزینه/اسپرد طلا
- ATR SL مناسب نوسان طلا
- رژیم شوک (ATR انفجاری)
- ورود پولبک به‌جای کراس خام
- partial TP و trailing نامتقارن
- سشن و کیفیت سیگنال
- بلاک اخبار پرریسک
- سقف تعداد معامله روزانه

## Changelog

### v1.9 — 2026-09-22
- ارتقا: قفل ورود بعد از دو ضرر متوالی در همان روز تقویمی.
- پایتون: `max_consecutive_losses_per_day=2` روی تاریخ واقعی ایندکس؛ بدون تاریخ، قفل اعمال نمی‌شود. شمارنده `bars_loss_lock_blocked`.
- EA: `MaxConsecutiveLossesPerDay=2` و شمارش DEAL_ENTRY_OUT امروز با همان Magic؛ دو خروج زیان‌ده پشت‌سرهم ورود جدید را تا پایان روز می‌بندد.
- فقط ورود جدید منع است؛ پوزیشن باز بسته نمی‌شود. ریسک 0.5% بدون تغییر.
- فیلترهای هزینه، SL کالیبره، شوک، pullback، کیفیت، trailing، اخبار و سقف روزانه حفظ شدند.
- مورد بعدی لوپ: کاهش ریسک بعد از ضرر متوالی (نصف ریسک معامله بعدی).

### v1.8 — 2026-09-22
- ارتقا: سقف ۲ معامله جدید در هر روز تقویمی UTC.
- پایتون: `max_trades_per_day=2` روی ایندکس زمانی واقعی؛ بدون تاریخ، سقف اعمال نمی‌شود (تاریخ جعلی ساخته نمی‌شود). شمارنده `bars_day_cap_blocked`.
- EA: `MaxTradesPerDay=2` و شمارش DEAL_ENTRY_IN امروز با همان Magic.
- فقط ورود جدید منع است؛ پوزیشن باز بسته نمی‌شود. ریسک 0.5% بدون تغییر.
- فیلترهای هزینه، SL کالیبره، شوک، pullback، کیفیت، trailing و اخبار حفظ شدند.
- مورد بعدی لوپ: قفل بعد از دو ضرر متوالی در همان روز.

### v1.7 — 2026-09-22
- ارتقا: بلاک ورود در پنجره اخبار پرریسک دلار/طلا.
- اگر ستون واقعی `news_high` در دیتاست باشد، هر بار که True است ورود جدید بسته می‌شود (تقویم خارجی واقعی؛ رویداد جعلی ساخته نمی‌شود).
- پروکسی زمان‌محور بدون تاریخ جعلی: جمعه ۱۲–۱۵ UTC (پنجره معمول NFP) و چهارشنبه ۱۸–۲۰ UTC (پنجره معمول FOMC).
- فقط ورود جدید منع است؛ پوزیشن باز بسته نمی‌شود. ریسک 0.5% بدون تغییر.
- فیلترهای هزینه، SL کالیبره، شوک، pullback، کیفیت و trailing حفظ شدند.
- مورد بعدی لوپ: سقف تعداد معامله روزانه (مثلاً ۲ معامله).

### v1.0
- قالب اولیه: روند EMA+ADX، squeeze BB، کراس EMA، RSI، ATR SL 1.8، ریسک 0.5%.
