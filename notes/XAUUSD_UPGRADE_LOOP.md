# حلقه ارتقای ربات طلا/دلار

اتومیشن روزانه باید:
1. فایل‌های فعلی `ea/GRK_XAUUSD_Gold_Dollar_EA.mq5` و `strategies/xauusd_gold_dollar_v1.py` را از ریپو بخواند.
2. یک ارتقای کوچک و قابل‌تست اعمال کند (نه بازنویسی کامل).
3. نسخه را افزایش دهد (1.0 → 1.1 → 1.2 ...).
4. changelog را در همین پوشه به‌روز کند.
5. به `main` پوش کند.
6. هیچ داده بازار جعلی نسازد و سود را تضمین نکند.

اولویت ارتقا:
- فیلتر هزینه/اسپرد طلا
- ATR SL مناسب نوسان طلا
- رژیم شوک (ATR انفجاری)
- ورود پولبک به‌جای کراس خام
- partial TP و trailing نامتقارن
- سشن و کیفیت سیگنال

## Changelog

### v1.5 — 2026-09-22
- ارتقا: امتیاز کیفیت سیگنال (0–3) قبل از ورود.
- یک نمره برای سشن هسته London/NY overlap (12–16 UTC اگر زمان موجود باشد).
- یک نمره برای شیب ADX صعودی: ADX[0] > ADX[1] و ADX >= adx_min.
- یک نمره برای انبساط BW بعد از squeeze: bw فعلی > bw قبلی (خروج از فشردگی).
- سیگنال فقط اگر quality >= MinQuality=2 پذیرفته می‌شود.
- فیلترهای هزینه، SL کالیبره، شوک و pullback حفظ شدند. ریسک 0.5% بدون تغییر. داده جعلی ساخته نشد.
- مورد بعدی لوپ: trailing نامتقارن.

### v1.4 — 2026-09-22
- ارتقا: ورود pullback به‌جای کراس خام EMA20/EMA50.
- روند کوتاه باید از قبل هم‌راستا باشد (EMA20 و EMA50 در دو کندل متوالی هم‌جهت).
- لانگ: Low به فاصله حداکثر `PullbackAtr=0.25 × ATR` از EMA50 برسد و Close بالای EMA20 بسته شود.
- شورت: High نزدیک EMA50 و Close زیر EMA20.
- هدف: جلوگیری از تعقیب کراس در سقف/کف و ورود نزدیک‌تر به میانگین میانی.
- فیلترهای هزینه، SL کالیبره و شوک حفظ شدند. ریسک 0.5% بدون تغییر. داده جعلی ساخته نشد.
- مورد بعدی لوپ: امتیاز کیفیت سیگنال (session + ADX slope + BW expansion).

### v1.3 — 2026-09-22
- ارتقا: فیلتر رژیم شوک — اگر ATR(14) > 2.0 × میانه ATR(50) ورود جدید بلاک می‌شود.
- هدف: جلوگیری از ورود در گسترش ناگهانی طلا (اخبار/گپ) که SL حتی با ضریب کالیبره هم راحت بریده می‌شود.
- پایتون: ستون `shock` و پارامتر `shock_atr_mult=2.0`؛ سیگنال فقط اگر `~shock`.
- EA: `ShockRegime()` و `ShockAtrMult` قبل از `Buy`/`Sell`.
- پوزیشن باز بسته نمی‌شود؛ فقط ورود جدید منع است. ریسک 0.5% بدون تغییر. داده جعلی ساخته نشد.
- مورد بعدی لوپ: ورود pullback به‌جای کراس خام.

### v1.2 — 2026-09-22
- ارتقا: کالیبراسیون ATR_SL طلا نسبت به میانه ATR(50).
- اگر ATR <= 0.85×میانه → ضریب 1.6 (SL تنگ‌تر در نوسان آرام).
- اگر ATR نرمال → ضریب 1.8.
- اگر ATR >= 1.40×میانه → ضریب 2.2 (فضای بیشتر در نوسان انفجاری).
- فیلتر هزینه همچنان روی فاصله SL واقعی (ضریب × ATR) اعمال می‌شود.
- ریسک 0.5% بدون تغییر. معامله زنده اضافه نشد. داده جعلی ساخته نشد.
- مورد بعدی لوپ: فیلتر رژیم شوک (ATR > 2× میانه).

### v1.1 — 2026-09-22
- ارتقا: فیلتر هزینه/اسپرد طلا.
- پایتون: سیگنال فقط اگر `SL >= 1.8 × spread` و `spread <= 0.25 × ATR`.
- اگر ستون `spread` واقعی در دیتاست باشد استفاده می‌شود؛ وگرنه `assumed_spread=0.30` (پارامتر هزینه، نه داده جعلی OHLCV).
- EA: `CostOk` اکنون نیز `MaxSpreadATRRatio` را چک می‌کند.
- ریسک همچنان 0.5% باقی ماند.
- مورد بعدی لوپ: کالیبراسیون ATR_SL (1.6 / 1.8 / 2.2).

### v1.0
- قالب اولیه: روند EMA+ADX، squeeze BB، کراس EMA، RSI، ATR SL 1.8، ریسک 0.5%.
