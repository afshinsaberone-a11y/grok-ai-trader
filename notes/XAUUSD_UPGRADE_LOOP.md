# حلقه ارتقای ربات طلا/دلار

اتومیشن روزانه باید:
1. فایل‌های فعلی `ea/GRK_XAUUSD_Gold_Dollar_EA.mq5` و `strategies/xauusd_gold_dollar_v1.py` را از ریپو بخواند.
2. یک ارتقای کوچک و قابل‌تست اعمال کند (نه بازنویسی کامل).
3. نسخه را افزایش دهد (1.0 → 1.1 → 1.2 ...).
4. changelog را در همین پوشه به‌روز کند.
5. به `main` پوش کند.
6. هیچ داده بازار جعلی نسازد و سود را تضمین نکند.

اولویت ارتقا:
- فیلتر هزینه/اسپرد طلا
- ATR SL مناسب نوسان طلا
- رژیم شوک (ATR انفجاری)
- ورود پولبک به‌جای کراس خام
- partial TP و trailing نامتقارن
- سشن و کیفیت سیگنال

## Changelog

### v1.2 — 2026-09-22
- ارتقا: کالیبراسیون ATR_SL طلا نسبت به میانه ATR(50).
- اگر ATR <= 0.85×میانه → ضریب 1.6 (SL تنگ‌تر در نوسان آرام).
- اگر ATR نرمال → ضریب 1.8.
- اگر ATR >= 1.40×میانه → ضریب 2.2 (فضای بیشتر در نوسان انفجاری).
- فیلتر هزینه همچنان روی فاصله SL واقعی (ضریب × ATR) اعمال می‌شود.
- ریسک 0.5% بدون تغییر. معامله زنده اضافه نشد. داده جعلی ساخته نشد.
- مورد بعدی لوپ: فیلتر رژیم شوک (ATR > 2× میانه).

### v1.1 — 2026-09-22
- ارتقا: فیلتر هزینه/اسپرد طلا.
- پایتون: سیگنال فقط اگر `SL >= 1.8 × spread` و `spread <= 0.25 × ATR`.
- اگر ستون `spread` واقعی در دیتاست باشد استفاده می‌شود؛ وگرنه `assumed_spread=0.30` (پارامتر هزینه، نه داده جعلی OHLCV).
- EA: `CostOk` اکنون نیز `MaxSpreadATRRatio` را چک می‌کند.
- ریسک همچنان 0.5% باقی ماند.
- مورد بعدی لوپ: کالیبراسیون ATR_SL (1.6 / 1.8 / 2.2).

### v1.0
- قالب اولیه: روند EMA+ADX، squeeze BB، کراس EMA، RSI، ATR SL 1.8، ریسک 0.5%.
